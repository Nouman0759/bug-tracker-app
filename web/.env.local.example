# Point this at the SAME backend the mobile app uses (same /api base path,
# same MongoDB behind it). Use your machine's IP or deployed URL as needed -
# "localhost" is fine here since the web app runs in a browser, not a phone.
NEXT_PUBLIC_API_URL=http://localhost:5000/api
